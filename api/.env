# Local environment variables for API (do NOT commit to VCS in production)
DB_HOST=127.0.0.1
DB_USER=root
DB_PASS=
DB_NAME=delicias_db
# Base URL where the API is accessible (used optionally to build absolute image URLs)
APP_URL=http://localhost/backend/api
